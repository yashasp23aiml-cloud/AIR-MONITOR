# AIR-MONITOR
This project is an IoT-based environmental monitoring and automatic control system using an ESP32. The system collects data from sensors and sends it to a laptop/API through Wi-Fi. The collected data can be displayed on a dashboard for real-time. Based on temperature and CO₂ values, the system can automatically control a fan and window mechanism.
